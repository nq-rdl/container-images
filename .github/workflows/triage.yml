name: Triage

permissions:
  contents: read
  packages: read

on:
  issues:
    types: [opened]
  pull_request_target:
    types: [opened, ready_for_review]

jobs:
  triage:
    if: github.event_name == 'issues' || github.event.pull_request.head.repo.full_name == github.repository
    uses: nq-rdl/.github/.github/workflows/auto-triage.yml@e20fa963eb48d59bad1060af89b694642eaddb7f
    with:
      domain: Infrastructure
    secrets: inherit
